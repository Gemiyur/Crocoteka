<Window x:Class="Crocoteka.Dialogs.FileInfoEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Crocoteka.Dialogs"
        mc:Ignorable="d"
        Title="Информация о файле"
        Width="500" Height="250" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="ButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="85" />
                <Setter Property="Height" Value="25" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Перевод" VerticalAlignment="Center" />
            <TextBox Grid.Row="0" Grid.Column="2" x:Name="TranslatorTextBox"
                     TextChanged="TranslatorTextBox_TextChanged" />

            <TextBlock Grid.Row="1" Grid.Column="0" x:Name="LectorTextBlock" Text="Читает"
                       VerticalAlignment="Center" Margin="0,10,0,0" />
            <TextBox Grid.Row="1" Grid.Column="2" x:Name="LectorTextBox" Margin="0,10,0,0"
                     TextChanged="LectorTextBox_TextChanged" />
        </Grid>

        <TextBlock Grid.Row="1" Text="Комментарий" Margin="0,10,0,2" />

        <TextBox Grid.Row="2" x:Name="CommentTextBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 TextChanged="CommentTextBox_TextChanged" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveButton" Content="Сохранить"
                    Style="{StaticResource ButtonStyle}"
                    IsEnabled="False"
                    Margin="0,0,10,0"
                    Click="SaveButton_Click" />
            <Button x:Name="CancelButton" Content="Отмена"
                    Style="{StaticResource ButtonStyle}"
                    Click="CancelButton_Click" />
        </StackPanel>

    </Grid>
</Window>
