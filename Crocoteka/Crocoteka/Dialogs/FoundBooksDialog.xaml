<Window x:Class="Crocoteka.Dialogs.FoundBooksDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Crocoteka.Dialogs"
        mc:Ignorable="d"
        Title="Файлы книг"
        Width="800" Height="450" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="ButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="75" />
                <Setter Property="Height" Value="25" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Папка -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"  Text="Папка" />
            <TextBox Grid.Column="1" />
        </Grid>

        <!-- Фильтр -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" x:Name="TypeComboBox" />
            <CheckBox Grid.Column="1" x:Name="NotInLibraryCheckBox" Content="Нет в библиотеке"
                      VerticalAlignment="Center" Margin="20,0,0,0" />
        </Grid>

        <!-- Список файлов -->
        <ListBox Grid.Row="2">
            
        </ListBox>

        <!-- Кнопки -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"  x:Name="FolderButton" Content="Папка"
                    Style="{StaticResource ButtonStyle}"
                    HorizontalAlignment="Left" />
            <Button Grid.Column="1" x:Name="BookButton" Content="Книга"
                    Style="{StaticResource ButtonStyle}"
                    IsEnabled="False"
                    HorizontalAlignment="Left" />
            <Button Grid.Column="3" x:Name="CloseButton" Content="Закрыть"
                    Style="{StaticResource ButtonStyle}"
                    IsCancel="True"
                    HorizontalAlignment="Right" />
        </Grid>

    </Grid>
</Window>
